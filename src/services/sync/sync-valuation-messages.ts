/** Mensaje cuando el autor local no tiene usuario central (cloud_user_id). */
export const VALUATION_SKIP_NO_CLOUD_USER_MESSAGE =
  'Esta cotización pertenece a un usuario local que aún no está registrado en el sistema central. Sincronice usuarios de campo desde Configuración.';
